When logging onto a new server do the following.
```
curl -u isaacdomini https://raw.githubusercontent.com/isaacdomini/dotfiles/master/setup.sh > setup.sh && bash setup.sh [config params]
```
#TODO
* add list of programs
* Config autoinstall 
* Arch packages
